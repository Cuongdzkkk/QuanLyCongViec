# Issue tracker: GitHub

Issues and specs for this repository live in GitHub Issues. Use the `gh` CLI for issue operations; it infers the repository from the current GitHub remote.

Pull requests are not a triage request surface.

When a skill says to publish a spec, create a GitHub issue. When it says to fetch a ticket, use `gh issue view <number> --comments`.
