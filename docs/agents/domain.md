# Domain docs

This repository uses a single-context documentation layout.

Before exploring a domain, read `CONTEXT.md` at the repository root when it exists and review relevant decisions under `docs/adr/` when that directory exists. These files are created lazily when domain terms or architectural decisions are established.
