FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src

COPY Backend/ Backend/

RUN dotnet restore Backend/src/TaskManagement.API/TaskManagement.API.csproj

RUN dotnet publish Backend/src/TaskManagement.API/TaskManagement.API.csproj \
    -c Release \
    -o /app/publish \
    --no-restore

FROM mcr.microsoft.com/dotnet/aspnet:10.0

WORKDIR /app

COPY --from=build /app/publish .

RUN mkdir -p /app/uploads /app/data-protection-keys \
    && chown -R app:app /app/uploads /app/data-protection-keys

ENV ASPNETCORE_URLS=http://+:8080

EXPOSE 8080

USER app

ENTRYPOINT ["dotnet", "TaskManagement.API.dll"]
